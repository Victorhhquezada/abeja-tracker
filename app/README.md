# ABEJA (app)

See the [repo README](../README.md) for what this project is and how it's structured.

Quick reference:

```bash
npm install
npm run build      # tsc -b && vite build
npx wrangler dev    # local dev server (app + API)
npx wrangler deploy # deploy to Cloudflare
```
